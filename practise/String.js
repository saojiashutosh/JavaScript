// take name as input and give username in the form @namename.length as output

let fullName = prompt("Enter your name without spaces:");

let userName = "@" + fullName + fullName.length;

console.log(`Your username is ${userName}.`)
